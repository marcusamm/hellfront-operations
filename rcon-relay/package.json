{
  "name": "rcon-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --env-file=.env server.js",
    "setup": "powershell -ExecutionPolicy Bypass -File ./setup.ps1"
  },
  "dependencies": {
    "fastify": "^5.2.1"
  }
}
